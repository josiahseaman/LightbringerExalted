__author__ = 'Josiah'
from django.conf.urls import patterns, url  # do not delete this

urlpatterns = patterns('',
                       # url(r'^Character/new/$', 'Character.views.new_chararacter'),
                       # url(r'^Character/(?P<primary_key>\d+)/$', 'Character.views.edit_character'),
                       # TODO: Edit by character name (repo name)
                       )